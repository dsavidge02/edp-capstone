import { Container, Form, Button } from "react-bootstrap";

const Home = (props) => {
  return (
    <Container className="home-page-container">
      <Container className="featured-container"></Container>
      <Container className="shop-container"></Container>
    </Container>
  );
};

export default Home;
